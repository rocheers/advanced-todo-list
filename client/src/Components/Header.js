import React from 'react'

export default ({title}) => {
  return (
    <div style={{ paddingTop: "1em", paddingBottom: "1em" }}>
      <h1 className="ui center aligned blue header">{title}</h1>
    </div>
  )
}
